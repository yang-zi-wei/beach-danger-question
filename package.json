{
  "name": "sea-guard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --host",
    "gen:assets": "node scripts/generate-assets.mjs",
    "optimize:images": "node scripts/optimize-images.mjs",
    "optimize:videos": "node scripts/optimize-videos.mjs",
    "deploy": "npm run optimize:images && npm run optimize:videos && vite build && gh-pages -d dist"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "howler": "^2.2.4",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "dotenv": "^16.4.5",
    "gh-pages": "^6.3.0",
    "sharp": "^0.34.5",
    "vite": "^5.2.0"
  }
}
